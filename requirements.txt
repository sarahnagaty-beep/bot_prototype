# The conversation engine, scoring, inventory matching and CLI run on the
# standard library alone. These are only needed for the service layer.
fastapi>=0.110
uvicorn>=0.27
requests>=2.28          # live WhatsApp Cloud API sends
python-dotenv>=0.20

# Development
pytest>=7.0
httpx>=0.27             # fastapi TestClient
